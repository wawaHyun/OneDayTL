설계도(class)에  따라 object를 만드는 것.
running 하는 것.
new 생성자를 통해 User라는 인스턴스를 user로 선언하고, 
User 인스턴스의 변수값에 user.name 이런식으로 접근하여 값을 지정해줌.

public class Main{
	public static void main(String[] args) {
		User user = new User();
		user.name = "jaja";
		user.phone = 010;
		user.job = "dev";
		}
}